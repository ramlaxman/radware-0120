[tool.poetry]
name = "ourpackage"
version = "0.1.0"
description = ""
authors = ["Reuven M. Lerner <reuven@lerner.co.il>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
